apiVersion: v1
kind: Service
metadata:
  name: haproxy
  namespace: aiops-sentinel
spec:
  type: NodePort
  selector:
    app: haproxy
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
